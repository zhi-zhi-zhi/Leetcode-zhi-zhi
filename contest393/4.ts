/**
 *
 * 3 <= points.length <= 10**5
 * points[i].length == 2
 * 1 <= points[i][0], points[i][1] <= 10**8
 *
 * 以每个点为起点，第二大曼哈顿距离点的最小值
 *
 * 困难点：在合适的时间复杂度内，求出每个点为起点的最大/次大曼哈顿距离d
 *
 */
function minimumDistance(points: number[][]): number {
  let res = 0

  return res
};