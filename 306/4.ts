function countSpecialNumbers(n: number): number {
  /*
  分别统计 [10^0 - (10^1 - 1)] .... [10^i - (10^(i+1)-1)] ... [10^j - n]
   */

  let res = 0
  const str = String(n)

  return res


  function dfs(i: number): number {

  }
}
